/**
 * State Management
 *
 * Global state stores will be added as needed.
 * Consider using Zustand or React Context depending on complexity.
 *
 * Planned stores:
 * - useAppStore      — Global app state (onboarding complete, etc.)
 * - usePersonStore   — Cached person data
 * - useMemoryStore   — Cached memory data
 */

export {};
